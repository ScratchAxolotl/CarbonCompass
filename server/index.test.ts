import { describe, it, expect } from 'vitest';

describe('Server Functionality', () => {
    it('should display when run', () => {
        const trueValue = true
        expect(trueValue).toBe(true);
    })
})